<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">
    <item android:id="@+id/check"
        android:icon="@drawable/check_all"
        android:title="@string/check_all" app:showAsAction="ifRoom" />
    <item android:id="@+id/trash"
        android:icon="@drawable/trash_icon"
        android:title="@string/action_trash" app:showAsAction="ifRoom" />
    <item android:id="@+id/add"
        android:icon="@drawable/add_icon"
        android:title="@string/add_item" app:showAsAction="ifRoom" />
</menu>
